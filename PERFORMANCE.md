# 加载与渲染优化 · 2026-09-07

本次降低首屏需要处理的 JavaScript、绘制调用和每帧重复计算，游戏与编辑器试玩共用优化后的场景。

## 测量结果

首屏关键 JavaScript 从 **905,290 字节降到 316,817 字节，减少 65.0%**。对应本地 gzip 体积从 256,256 降到 107,132 字节，减少 58.2%。菜单新增 15,332 字节的 WebP 场景快照；Three.js 场景在开局或进入房间前预备加载。

同一台机器、同一浏览器，画布 CSS 尺寸 960 × 540，每组 180 帧，前 30 帧预热，固定 1/60 秒步长，第 100 帧切换视角。CPU 耗时是 `PaperScene.render()` 的平均执行时间。

| 场景           | 每帧 CPU 耗时：优化前 → 后 | 平均绘制调用：优化前 → 后 | 几何资源：优化前 → 后 |
| -------------- | -------------------------- | ------------------------- | --------------------- |
| 单人，第 1 关  | 3.98 → 2.35 ms             | 1027 → 369                | 712 → 315             |
| 六人，第 11 关 | 5.11 → 2.71 ms             | 1324 → 554                | 976 → 530             |
| 六人，第 12 关 | 5.13 → 2.70 ms             | 1284 → 531                | 948 → 485             |

三组每帧 CPU 耗时减少约 41%–47%，绘制调用减少约 58%–64%。测试机器前后都维持约 16.67 ms 的帧间隔，因此结果体现为更多运行余量；不代表已经测得手机帧率或真实网络下加载秒数的同等改善。

原始记录见 `benchmarks/results-2026-09-07.json`。运行 `npm run dev` 后打开 `/benchmarks/render.html`，点击「运行渲染基准」可复测。对比时关闭其他试玩页，保持相同窗口和设备条件；基准页面不属于生产打包入口。

## 实施内容

- 菜单用游戏场景生成的小体积快照，移除菜单持续运行的 WebGL 与定时 HUD 更新。3D 模块按需加载，支持预热和加载失败提示。
- 平台顶面和侧面使用顶点颜色，共用一个材质绘制；许愿架的静态部件合并，挂签继续独立摆动。保留原有光照、阴影、平台尺寸和碰撞规则。
- 固定屋檐、火堆棚顶、平台对每滴雨的遮挡在场景建立时计算。每帧只检查移动平台与展开挡雨的玩家。
- 仅计算正在显示的纸面形变；复用向量和颜色，缓存路牌引用，平台透明状态变化时才更新材质。
- 游戏和试玩最多绘制 60 帧/秒，暂停说明时降至 30 帧/秒；隐藏页面停止渲染和本地模拟，恢复时不补算离开期间的时间。物理仍使用 1/60 秒步长。
- 首次游戏数据与音乐偏好只初始化一次；地图边界按关卡缓存；联机房间信息只在修订变化时刷新，预测前保留独立快照；音乐面板关闭时不重复刷新播放进度。

## 验证

- 188 项单元测试通过，包括高刷新率节奏、合并前后颜色与空间范围、挂签动画结构，以及 12 关共 1,200 组雨滴遮挡结果对比。
- 本地真实 WebSocket 回归：六人第 12 关的状态、座位、视角与重连，以及退出后的连接撤销通过。
- 实际双人网页开局、共享转面通过；单人开局、说明暂停恢复、返回菜单后换关、编辑器第 11 关试玩通过，所检查页面无控制台错误。
- 390 × 844 手机视口的菜单、触控按钮与转面通过；补齐首次渲染至监听订阅之间发生窗口变化时的布局同步。
- 严格 TypeScript 检查与生产构建通过。Three.js 场景仍有大包提示，但已经移出首屏必须下载的模块。
