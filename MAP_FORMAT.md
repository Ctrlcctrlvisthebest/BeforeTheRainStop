# 雨停之前 · 地图文件与编辑器

游戏：`/BeforeTheRainStop/`；独立编辑器：`/BeforeTheRainStop/editor.html`。

编辑器可以从空白小径或现有 12 关开始，在俯视、正面、侧面三种视图中放置和拖动物件。支持精确属性、网格吸附、平移缩放、复制删除、80 步撤销重做、草稿自动保存、导入 JSON、导出 JSON 和单人试玩。试玩共用游戏的物理、天气、纸桥、耐折、存档和引导代码。

草稿保存在当前浏览器的 `rain-map-editor-v1` 中。结构存在错误时保留上一次有效草稿，界面会提示修复。请导出文件作为可迁移的备份。导入失败不会替换当前地图。地图文件不包含脚本、账号或联机房间。

## 格式

文件建议命名为 `名称.rain-map.json`，UTF-8 JSON，最大 500 KB：

```json
{
  "format": "before-the-rain-map",
  "version": 1,
  "level": {
    "name": "我的地图",
    "sub": "一段新的雨中小径。",
    "hint": "左右移动 · 空格跳跃 · Q 转面",
    "color": "#53697b",
    "sky": "#28394b",
    "spawn": { "x": -1, "y": 0, "z": 0 },
    "exit": { "x": 9, "y": 0, "z": 0 },
    "platforms": [{ "x": 4, "y": 0, "z": 0, "w": 14, "d": 4, "h": 1 }],
    "keys": [], "stars": [], "checkpoints": [], "signs": [],
    "pads": [], "winds": [], "hazards": []
  },
  "weather": {
    "period": 10, "zones": [],
    "awnings": [{ "x": 0.5, "y": 3.4, "z": 0, "w": 7, "d": 3.8 }]
  },
  "route": [{ "kind": "exit", "target": { "x": 9, "y": 0, "z": 0 }, "view": 0 }]
}
```

正式第 9–12 关在 `src/maps/*.json` 中，直接消费这个格式。编辑器导出的文件无需再转换地形、天气或引导。

## 坐标与对象

- `x` 左右、`z` 前后、`y` 高度。正面右键沿 +X；侧面右键沿 −Z。
- 平台的 `y` 是顶面高度，`h` 是向下的厚度；`w` / `d` 是完整宽度和深度。
- 平台可选 `kind`: `wall`、`step`、`moving`、`low-roof`、`railing`。它是用途 / 外观标记，不自动改变长方体的尺寸；`step` 不会生成楼梯。编辑器选择渡台种类时会同时开启运动，手写 JSON 则仍需明确提供 `motion`。
- `motion: {axis: "x" | "z", range, period}` 使平台在游戏开始后自动按正弦往返，站稳的纸鹤会被带着移动。平台 X/Z 是轨迹中心，range 为到一端的单侧距离，period 是完整一轮的秒数。例如 X=10、range=2、period=6：中心在 8–12 之间移动，6 秒循环一次；Y 不变。编辑器提供播放与时间拖动示意。
- 起点、终点、钥匙、星星、存档架、开门踏板使用 `{x,y,z}`。踏板带门形标记，四格灯显示开门计时，字段仍为 `pads`。钥匙常放在平台上方 0.7。存档点按数组顺序递进，死亡会回滚上个存档之后拾取的物品。
- 路牌额外支持 `text` 和 `view: 0 | 1`；不填 view 两面可见。
- 旺火为 `{x,y,z,w,d,period?}`，y 为地面高度。无周期时常燃；有周期时前 48% 为旺火，后 52% 熄灭。每个旺火自动生成篷子，篷子不会生成额外小火。
- 风柱为 `{x,y,z,w,d,height}`，y 是底部，height 为升高范围。
- 地图最多一道 `level.gate`，形状与平台相同。最多两块 `pads`，单人占一块，多人占两块，持续四秒开门。
- 纸桥为可选顶层 `crossing: {x,y,z,axis,near,span,depth}`。每图一处；near 为 -1 或 1，span 最大 2.8。编辑器的纸桥组件自动生成两岸、低檐与侧栏；游戏按 crossing 自动生成两岸桥钉、对岸接桥踏板与木桥，单人不需要专门添加桥钉或开门踏板。单人保持 Shift 2 秒接桥，多人需要队友从纸面过桥后在对岸站稳 2 秒。
- 组件的断口与五块支撑 / 限制平台是独立对象。改变断口后，要同时调整两岸、低檐、侧栏和 bridge 引导；不要用其他平台铺满缺口，否则可以直接走过。

## 天气与引导

`weather.period` 为雨势周期。`zones` 使用 `{x,z,w,d,rate}`；rate 为每秒基础湿度，周期前 38% 乘 1.45，其余乘 0.75。重叠雨区取最大值。

`awnings` 使用 `{x,y,z,w,d,floor?}`，y 为篷顶，floor 为小火所在平台的高度（默认 0）。每个篷子生成一堆小火，第一座的火放在左后方以避开出生位置。檐下只停止淋雨，小火范围内才能烤干，久留会变脆。

`route` 按玩家行进顺序排列，最后一项必须是 exit。每步含 `kind`、`target: {x,y,z}`、`view: 0 | 1`。

| kind | 用途与额外字段 |
| --- | --- |
| walk / jump | 走到或跳到 target |
| rack / key | `id` 对应 checkpoints / keys 的数组下标 |
| bridge | 必须有 crossing，target 是对岸 |
| wind | `from: {x,y,z}` 是风柱底部，target 是上方落脚平台 |
| ferry | `id` 指向有 motion 的平台，target 是对岸；可选 requiredKey 指向必须乘船拿到的钥匙 |
| pads | 必须存在开门踏板 |
| exit | 指向终点 |

编辑器会自动维护钥匙、存档架、终点的引导坐标，并在删除对象时修正索引。走路、跳跃、转弯的中间点需要手动安排。正面与侧面引导决定玩家何时换轴。

可选 `translations` 是中文原文到英文译文的对象，用于名称、副标题、提示和路牌。没有翻译的文字保留原文。

## 加入正式游戏

1. 在编辑器试玩并导出，把 JSON 放入 `src/maps/`。
2. 在 `src/challenge-maps.ts` 导入文件，并追加到 `CHALLENGE_MAPS` 的原始文件数组中，例如 `import custom from "./maps/13-custom.rain-map.json"`。保持已有文件的顺序，房间用关卡索引保存进度。
3. `LEVELS`、天气、纸桥、引导、翻译会自动从文件注册。无需分别改五处数据。
4. 为地图补充正常输入通关路线与测试，运行 `npm test`、`npm run build`。多人地图还需验证桥的接应、开门踏板、六人起点及等待区。
5. 同步发布 GitHub Pages 和联机 Worker，让前端与服务端拥有相同关卡表。

结构校验检查类型、数值范围、数量和引用。支撑平台、六人起点空间、缺失钥匙引导等会给出提醒；它不能证明所有路径可通关。编辑器当前提供单人试玩，正式地图支持 1 / 2 / 3 / 6 人，编辑器不创建在线自定义房间。
