# 代码检查与防呆修复 · 2026-09-07

检查范围：游戏入口与输入、地图格式及编辑器、试玩生命周期、Three.js 场景资源、房间客户端和 Cloudflare 联机服务。

## 已修复

| 问题 | 调整 |
| --- | --- |
| 游戏和试玩分别维护按键集合、映射及延迟释放，暂停后可能恢复旧按键 | 共用 `KeyboardInput` / `bindGameKeyboard`；暂停、说明、切换场景、失焦和退出时清空；仅跳跃、转面、回存档保留短点按脉冲，折纸、挡雨、修补即时松开 |
| 两个方向的别名键可能重复计数 | 同方向按一次计数，左右同时按抵消，松开一个别名不会松开另一个 |
| 已关闭客户端连接的迟到回调仍能更新界面 | 所有回调检查连接是否仍有效；关闭时解除回调并取消心跳与重连 |
| 自动恢复旧房间的请求晚于用户新开局返回 | 恢复请求绑定操作序号；用户开始新操作后旧恢复结果失效 |
| 只按座位编号识别连接，离开后旧连接可能影响复用座位 | WebSocket 附件绑定玩家凭证；HTTP 和 WebSocket 共用命令处理；离开即撤销连接和输入；断开通知校验身份；服务重新初始化时按实际连接恢复在线状态 |
| 服务端接受数组形式消息，内部异常可能直接发给玩家 | 拒绝非对象消息和命令，返回可读错误；意外异常只记录在服务端 |
| 地图验证通过后仍保留未知属性，错误的 `target` 或 `motion` 可破坏通用属性面板 | 导入只复制格式中支持、已经校验的字段；错误的引用编号在索引前先判断类型 |
| 失效选区可能删除最后一个物件，或让复制操作抛异常 | 无效编号、过期选区安全返回；起点、终点及末尾出口引导在模型层保护 |
| 复制可以绕过两块踏板限制，组件可一次突破平台或引导数量上限 | 数量上限共用一份定义；添加和复制在提交前整体检查，失败保留原图并显示原因 |
| 改引导种类后残留钥匙前置条件和旧引用 | 按新的动作清理 `id`、`requiredKey`、`from`；出口引导保持唯一且位于最后 |
| 添加机关门没有开门引导，移动桥或踏板后引导仍指旧位置 | 自动生成开门步骤；纸桥目标与方向、踏板目标同步；删除门清理开门引导 |
| 高处纸桥的桥钉、踏板、木桥和低檐仍画在零高度 | 显示位置以实际桥面或碰撞底面计算；低檐和侧栏共用高度换算 |
| 场景重建与退出各写一套释放代码，共享材质可能重复释放 | 统一释放几何、材质和纹理，每个共享资源只释放一次 |
| 试玩退出或启动失败后留下临时关卡注册 | 注册提供对应清理，退出、重开和 WebGL 启动失败均恢复所有临时表项 |
| 浏览器存储属性本身被禁用时，原有 try/catch 无法捕获 | 在保护范围内取得存储对象；损坏的名字、会话和音量使用类型校验或默认值 |
| 三处移动平台默认参数重复，9 处未使用导入、参数或成员 | 集中默认移动参数，移除无用声明；TypeScript 构建开启 `noUnusedLocals`、`noUnusedParameters` |

## 验证

- `npm run types`：通过，Worker 绑定使用生成类型。
- `npm test`：180 项通过，包含 24 项新增测试；异常地图字段矩阵覆盖超过 1,000 次替换，包括错误数组、空值、越界值和不可转换的引用对象。
- `npm run test:integration`：17 项通过。真实本地 Worker / WebSocket 验证 2、3、6 人同步、座位所有权、重连、纸桥接应、过热失败、掉线暂停、物品复位与存档；新增两种退出途径的旧连接撤销和座位复用测试。
- `npm run build`：通过，含严格 TypeScript 检查与生产打包。
- 浏览器实测：开门组件与引导生成、复制第三块踏板被拦截、终点删除保护、试玩暂停时忽略按键、恢复转面、重复重开及返回试玩、中文/英文切换、单人游戏与说明窗口；检查页未出现控制台错误。
- 游戏 390 × 844 视口：控制按钮完整，页面宽度等于视口宽度，没有横向溢出。
- 检查源文件中连续 12 行、去缩进后超过 300 字符的完全重复片段：未发现残留组；另人工核对了行为相近的输入、移动参数、房间命令和资源释放代码。

## 边界

地图结构校验仍需结合试玩确认跳跃距离与整条路线可达性。纸桥两岸、低檐和侧栏作为独立平台编辑，改变断口后需要调整这些平台。Vite 仍提示 Three.js 场景包大于 500 kB；本次构建成功，此提示不影响发布。

本轮测试覆盖上述故障与已有玩法回归，不代表穷尽所有浏览器、网络时序或玩家自制地图组合。
